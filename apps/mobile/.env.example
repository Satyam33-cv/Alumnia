# Mobile API URL
# - Development (local API): leave empty → auto-uses 10.0.2.2 (Android emulator) or localhost (iOS sim)
# - Production build: EAS Build injects API_URL from eas.json (already set to your deployed API)
API_URL=
